import React from "react";
import { Animated } from "react-native";
export declare const FastListSectionRenderer: ({ layoutY, layoutHeight, nextSectionLayoutY, scrollTopValue, children, }: {
    layoutY: number;
    layoutHeight: number;
    nextSectionLayoutY?: number;
    scrollTopValue: Animated.Value;
    children: React.ReactNode;
}) => JSX.Element;
